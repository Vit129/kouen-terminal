# CASE — Testing Infrastructure

Grep target: `grep -n "CASE-\|<keyword>" knowledge/cases/testing-infra.md`

| ID | Trigger | Fix |
|----|---------|-----|
| CASE-065 | `Tests/KouenRobotTests/` suites (real UI-driving robot tests via macOS accessibility, distinct from `Tests/robot/`'s source-pattern-check suites) fail at **Test Setup**, before reaching any assertion: `open -b com.robert.kouen.preview` → `LSCopyApplicationURLsForBundleIdentifier() failed` | **OPEN, not yet fixed.** Confirmed 2026-07-11 (P39 session) via an actual `robot` run against `p16_board.robot`, not just static reading. Root cause not yet isolated — `com.robert.kouen.preview` looks like a stale bundle ID from before the project's rename (harness → kouen; an old path `/Users/supavit.cho/Git/harness-terminal/...` surfaced separately in the same session). Separately, **zero** `SwiftUI .accessibilityIdentifier(` calls exist anywhere in `Apps/Kouen/Sources/KouenApp` (only 6 AppKit `setAccessibilityIdentifier` calls, all in `BrowserPaneView.swift`) — so even past the launch failure, identifier-based cases like `Board Tab Accessible In Sidebar`/`Board Columns Visible After Click` (querying `sidebar-tab-board`/`board-column-idle` via `AXIdentifier`) would still fail; those identifiers were never wired into source. The README's own citation for "the full list" of required identifiers, `agent-memory/plans/p18-ui-automation.md`, does not exist in the repo (deleted or never committed). This predates P39 entirely — not something that session's diff broke. **Before adding any new suite here**: fix the bundle-ID launch path first (check `Info.plist`/`prepare-release.sh` for the current real bundle ID vs what `KouenUILibrary.py`/`kouen.resource` hardcode), then decide whether to resurrect true SwiftUI accessibility identifiers or rewrite the affected suites to be keyboard-shortcut + CLI-verification only (the pattern several other cases in the same suites already use successfully, e.g. `Board CLI Shows Columns`). |
